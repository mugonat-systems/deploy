<?php

namespace Deployer;

desc('NPM install & build for production');
task('deploy:node_modules', function () {
    cd('{{release_or_current_path}}');
    run('npm install');
    run('npm run build');
});

after('deploy:vendors', 'deploy:node_modules');
